export enum Device {
  phone = "phone",
  desktop = "desktop",
}

export enum Theme {
  light = "light",
  dark = "dark",
  auto = "auto",
}

export enum CursorEffect {
  follow = "圆点跟随",
  take = "圆点",
  none = "无特效",
}
