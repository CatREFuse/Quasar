export enum Device {
  phone = "phone",
  desktop = "desktop",
}

export enum Theme {
  light = "light",
  dark = "dark",
}

export enum CursorEffect {
  follow = "跟随",
  take = "替代默认鼠标",
  none = "无特效",
}
